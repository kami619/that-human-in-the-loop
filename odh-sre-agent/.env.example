# Claude API (required — used by claude-agent-sdk under the hood)
ANTHROPIC_API_KEY=sk-ant-...

# Path to kubeconfig (optional — defaults to ~/.kube/config)
KUBECONFIG=

# Default namespace for queries (optional — defaults to all namespaces)
SRE_DEFAULT_NAMESPACE=
